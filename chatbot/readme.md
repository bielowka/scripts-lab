For DISCORD_TOKEN, which should be added es env variable, contact the repository owner

To start:
 - Run python bot.py
 - Run 'rasa run --enable-api' in the terminal
 - Go to actions folder and run 'rasa run actions' in another terminal